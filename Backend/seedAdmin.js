const mongoose = require('mongoose'); const User = require('./models/User'); mongoose.connect('mongodb://127.0.0.1:27017/lms_db').then(async () => { const bcrypt = require('bcryptjs'); const salt = await bcrypt.genSalt(10); const password = await bcrypt.hash('12121212', salt); await User.findOneAndUpdate({email: 'a.chfrn@gmail.com'}, {name: 'Admin Farhan', role: 'Admin', password: password}, {upsert: true, new: true}); console.log('Admin seeded!'); process.exit(0); });
