const mongoose = require('mongoose'); const User = require('./models/User'); mongoose.connect('mongodb://127.0.0.1:27017/lms').then(async () => { const bcrypt = require('bcryptjs'); const salt = await bcrypt.genSalt(10); const password = await bcrypt.hash('11111111', salt); await User.findOneAndUpdate({email: 'a.chfrn@gmail.com'}, {name: 'Admin', role: 'Admin', password: password}, {upsert: true, new: true}); console.log('Admin seeded with 11111111!'); process.exit(0); });
